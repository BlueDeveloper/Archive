# 2026-04-26 작업 내역

## 1. 대시보드 TeleQuote(택군) 프로젝트 업데이트
- D1 DB에서 기존 프로젝트(id: 63) 데이터 업데이트
- folder: `Soomgo/11.telequote/telequote`, type: 외주, platform: PC
- deploy_method: Cloudflare Pages, tech_stack: Next.js/TypeScript/CSS Modules
- contract_date: 2026-04-24, note: 숨고 수주/바로폼 벤치마킹

## 2. 프로젝트 등록 폼 UX 개선
- **플랫폼**: 텍스트 입력 → 칩 버튼 6개 (PC, 모바일, PC+모바일, 앱 등)
- **배포 방법**: 텍스트 입력 → 칩 버튼 6개 (Cloudflare Pages, Vercel, OCI 등)
- **기술스택**: 프리셋 버튼 20개 추가 (클릭 토글) + 직접 입력 유지

## 3. Cloudflare 계정 강제 규칙 설정
- **문제**: wrangler가 프로젝트별 캐시에 따라 다른 계정(raon_foodtruck)으로 리소스 생성
- **원인**: 환경변수 미설정 + 프로젝트별 `.wrangler/cache/wrangler-account.json` 캐시 충돌
- **해결**:
  - CLAUDE.md(전역) + INFRASTRUCTURE_GLOBAL_REFERENCE.md에 Cloudflare 계정 강제 규칙 추가
  - `.bashrc` + PowerShell Profile에 `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` 영구 설정
  - 토큰 통일: `cfat_` (Account Token) → `cfut_` (User Token) 전체 교체
  - 피드백 메모리 저장

## 4. 대시보드 디자인 — 라이트 테마 전환
- **다크 → 라이트** 전체 색상 체계 변경
  - 배경: `#0f1117` → `#f0f2f5`
  - 카드: `#1a1d27` → `#ffffff`
  - 보더: `#2a2e3d` → `#dfe2e8`
  - 텍스트: `#e4e6ed` → `#1a1d2b`
- Nav 버튼 가시성 개선 (배경색 + active accent)
- 캘린더 nav 버튼 accent 배경 적용
- 카드 전체 box-shadow 추가
- 하드코딩 다크 rgba 전부 라이트 대응으로 교체
- `input[type="date"]` 캘린더 아이콘 invert 필터 제거 (라이트에서 불필요)

## 5. 보안/버그 감사 (Audit)
### API 보안 수정
- **모든 PUT 라우트 필드 주입 차단**: projects, timelines, settlements, expenses, work-hours
  - `{ id, ...data }` spread → 화이트리스트 필드 필터로 변경
  - 임의 필드(id, createdAt 등) DB 주입 불가

### 버그 수정
- `daysUntil()`: 잘못된 날짜 입력 시 `NaN` → `0` 반환
- `ProjectCard`: settlement 변경 실패 시 이전 상태 정확히 복원 (레이스 컨디션)
- `ProjectCard`: API 응답 `res.ok` 체크 추가
- `Nav`: 로그아웃 fetch 에러 핸들링 추가

### 확인된 저위험 이슈 (현 단계 수정 불필요)
- SHA-256 비밀번호 해싱 — 단일 사용자 대시보드, 허용 수준
- N+1 쿼리 — D1 특성상 관계 쿼리 제한적, 현재 데이터 규모 문제 없음
- CSRF — httpOnly + sameSite: strict 쿠키로 방어됨

---

## 커밋 이력
1. `17569891` — 프로젝트 등록 폼 UX 개선
2. `b2c36fde` — 대시보드 전체 가시성/가독성 개선
3. `8c728732` — 날짜 입력 캘린더 아이콘 다크모드 대응
4. `5f2ce301` — 캘린더 아이콘 완전 흰색으로 변경
5. `b32ad62a` — 캘린더 아이콘 CSS Modules 스코프 내 직접 적용
6. `3b7240fd` — 대시보드 라이트 테마 전환
7. `122b0857` — 보안/버그 감사 수정
