# 2026-04-24 작업 로그

## 작업 내역

### Dashboard 검수 및 수정 (20개 이슈)
- Nav.tsx 로그아웃 버튼 추가 (DELETE /api/auth → 로그인 리다이렉트)
- Finance.tsx + Finance.module.css 죽은 코드 삭제
- DB schema에 Drizzle relations + projectId 인덱스 추가
- 5개 API의 `as never` 제거 + InferInsertModel 타이핑 + POST 입력 검증
- lib/data.ts 에러 핸들링 개선 (에러 UI 표시)
- Stats.tsx `totalProfit` → `totalRevenue` 변수명 정리
- 로그인 에러 분기 (401/400/서버에러/네트워크 각각 다른 메시지)

### D1 데이터 수정
- 목업 사이트 제작(한정민) 금액: 549,998 → 590,000 (Cloudflare D1 API 직접 수정)

## 미해결 이슈
- [ ] expenses, settlements, timelines, workHours 스키마에 updatedAt 컬럼 없음 → 마이그레이션 필요
- [ ] Rate limiting, CSRF 보호, 페이지네이션 미적용 (3차 개선)
- [ ] 보안 헤더 미설정
